<?xml version="1.0"?>
<root BTCPP_format="4">
  <BehaviorTree ID="MainTree">

    <RecoveryNode number_of_retries="6" name="NavigateRecoveryRoot">

      <PipelineSequence name="DynamicNavigation">

        <RateController hz="0.65">
          <RecoveryNode number_of_retries="1" name="ComputePathRecovery">
            <Sequence name="ComputeAndSmoothPath">
              <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
              <SmoothPath unsmoothed_path="{path}" smoothed_path="{path}"/>
            </Sequence>
            <ClearEntireCostmap name="ClearGlobalCostmap-OnPlanFail"
                                service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>

        <RecoveryNode number_of_retries="2" name="FollowPathRecovery">
          <FollowPath path="{path}" controller_id="FollowPath"/>

          <Sequence name="SoftRecovery">
            <ClearEntireCostmap name="ClearLocalCostmap-FollowFail"
                                service_name="local_costmap/clear_entirely_local_costmap"/>
            <Wait wait_duration="1.0"/>
          </Sequence>
        </RecoveryNode>

      </PipelineSequence>

      <RoundRobin name="PhysicalRecovery">

        <Sequence name="TryShortBackup">
          <ClearEntireCostmap name="ClearLocal1" service_name="local_costmap/clear_entirely_local_costmap"/>
          <BackUp backup_dist="0.15" backup_speed="0.08"/>
          <Wait wait_duration="0.8"/>
        </Sequence>

        <Sequence name="TryLongBackup">
          <ClearEntireCostmap name="ClearLocal2" service_name="local_costmap/clear_entirely_local_costmap"/>
          <BackUp backup_dist="0.35" backup_speed="0.08"/>
          <Wait wait_duration="1.0"/>
        </Sequence>

        <Sequence name="TryGlobalAndLocalClear">
          <ClearEntireCostmap name="ClearGlobal2" service_name="global_costmap/clear_entirely_global_costmap"/>
          <ClearEntireCostmap name="ClearLocal3" service_name="local_costmap/clear_entirely_local_costmap"/>
          <Wait wait_duration="1.5"/>
        </Sequence>

        <Sequence name="TrySpinAsLastResort">
          <ClearEntireCostmap name="ClearLocal4" service_name="local_costmap/clear_entirely_local_costmap"/>
          <Spin spin_dist="0.8"/>
          <Wait wait_duration="1.0"/>
        </Sequence>

        <Sequence name="TryWait">
          <Wait wait_duration="1.5"/>
        </Sequence>

      </RoundRobin>

    </RecoveryNode>

  </BehaviorTree>
</root>
